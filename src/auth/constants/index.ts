export enum TYPE_TRIED {
  fail = 'Failed Login',
  success = 'Success Login',
}

export enum MESSAGE {
  userNoFound = 'User not found',
  validPassword = 'The password must have a Uppercase, lowercase letter and a number',
  validCredential = 'Credentials are not valid',
  checkLog = 'Please check server logs',
}
